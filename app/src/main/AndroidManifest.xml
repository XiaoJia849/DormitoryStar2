<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dormitorystar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DormitoryStar">
        <activity
            android:name=".UserInfoEditActivity"
            android:exported="true" />
        <activity
            android:name=".CalenderActivity"
            android:exported="true" />
        <activity
            android:name=".PlanChooseCreateActivity"
            android:exported="true" />
        <activity
            android:name=".MemberLeaderElectActivity"
            android:exported="true" />
        <activity
            android:name=".LoginOrRegisterActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>